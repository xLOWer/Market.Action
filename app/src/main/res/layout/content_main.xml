<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="ru.xlower.marketaction.MainActivity"
    tools:showIn="@layout/app_bar_main">

    <Button
        android:id="@+id/buttonGet"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="onClick_buttonGet"
        android:text="GET api/goods HTTP/1.1"
        app:layout_anchorGravity="top|center" />

    <EditText
        android:id="@+id/testField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:hint="debug message"
        android:inputType="textMultiLine"
        android:padding="8dp"
        android:textSize="12sp"
        app:layout_anchor="@+id/include"
        app:layout_anchorGravity="top|center"
        app:layout_constraintTop_toBottomOf="@+id/buttonGet" />

</android.support.constraint.ConstraintLayout>
